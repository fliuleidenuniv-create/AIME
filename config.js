// WARNING: Do not commit real API keys to the repository.
// Recommended workflow:
// 1. Copy `config.example.js` to `config.local.js`.
// 2. Put your real keys in `config.local.js`.
// 3. `config.local.js` is listed in .gitignore and will not be committed.

// Default placeholder config used if you don't have a local config file.
const CONFIG = {
  GOOGLE_API_KEY: '' // Place your real Google API key here
};

// If you create a `config.local.js`, it should overwrite these values. Example:
// const CONFIG = { GOOGLE_API_KEY: '...', OPENAI_API_KEY: '...' };
